<% layout("/layouts/adminboilerplate") %>
  <body>
    
    <div class="container-fluid">
      <div class="row">
        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
          <h1>All Booking</h1>

          <div class="table-responsive">
            <table class="table table-striped table-sm">
              <thead>
                <tr>
                  <th scope="col">Booking Id</th>
                  <th scope="col">Full Name</th>
                  <th scope="col">Email</th>
                  <th scope="col">Guest Size</th>
                  <th scope="col">Phone</th>
                  <th scope="col">Price</th>
                  <th scope="col">Location</th>
                  <th scope="col">Country</th>
                  <th scope="col" class="text-center">Action</th> 
                </tr>
              </thead>
              <tbody>
                <% for(let booking of allbooking){ %>
                  <tr>
                    <td><%= booking._id %></td>
                    <td><%= booking.fullName %></td>
                    <td><%= booking.userEmail %></td>
                    <td><%= booking.guestSize %></td>
                    <td><%= booking.phone %></td>
                    <td><%= booking.price%></td>
                    <td><%= booking.location %></td>
                    <td><%= booking.country %></td>
                    <!-- <td>
                        <button class="btn  checking-btn"><b>Pandding</b></button>
                    </td> -->
                    <td>
                      <div class="buttons">
                        <form
                          method="post"
                          action="/admin/showbooking/<%= booking._id %>?_method=DELETE">
                          <button class="btn btn-danger delete-btn">Delete</button>
                        </form>
                      </div>
                    </td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>
          <div class=" page-container mb-3 ">
          
        </div>
        </main>
      </div>
    </div>
  </body>

  <!-- <script>
    let checkingBtns = document.querySelectorAll(".checking-btn");
    console.log(checkingBtns);
    for(btn of checkingBtns){
      btn.addEventListener("click",function(){
        if(btn.innerText == "Pandding"){ 
          btn.innerText = "Conformed";
        }
      });
    }
    //   checkingBtns.addEventListener("click",function(){
    //   checkingBtns.innerText = "Conformed";
    // });
    
  </script> -->
  